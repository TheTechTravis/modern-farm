// Accepts the year's planting plan as input.
// The plan is an array. It contains 4 arrays representing 
// the rows in the field to be plants. Figure out how to iterate 
// both the parent array and the child arrays.

export const plantSeeds = (yearlyPlan) => {
    
}